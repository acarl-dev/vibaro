# Canonical public API URL used by the web app for backend requests.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Canonical public web URL used to build absolute links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Legacy only: do not use for new code.
# Keep temporarily if old code still references it.
# NEXT_PUBLIC_WEB_URL=http://localhost:3000
